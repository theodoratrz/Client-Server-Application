#ifndef CHECKARGUMENTS
#define CHECKARGUMENTS

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// check if passing arguments are ok
int checkArguments(int flag, int argc, char const *argv[], char** args);

#endif